import { useState } from 'react'; import { auth, postJson } from './api'; import { Icon } from './Icons'; import type { AuthResponse } from './types'
export default function AuthPanel(){const[mode,setMode]=useState<'login'|'register'>('login'),[email,setEmail]=useState(''),[password,setPassword]=useState(''),[error,setError]=useState<string|null>(null),[busy,setBusy]=useState(false);async function submit(e:React.FormEvent){e.preventDefault();setBusy(true);setError(null);try{const grant=await postJson<AuthResponse>(mode==='login'?'/api/v1/auth/login':'/api/v1/auth/register',{email,password});auth.save(grant);window.location.reload()}catch(err){setError(err instanceof Error?err.message:'Something went wrong');setBusy(false)}}return <main className="auth-page"><section className="auth-story"><div className="brand-lockup light"><span className="brand-mark"><Icon name="sparkle"/></span><span>PayPilot</span></div><div className="story-copy"><span className="eyebrow light">Commerce, on autopilot</span><h1>Your smartest way<br/>to <em>shop.</em></h1><p>Tell PayPilot what you need. It finds, compares, and prepares the purchase—while you stay in control of every rupee.</p></div><div className="trust-row"><span><Icon name="shield"/> Approval first</span><span><Icon name="lock"/> Secure by design</span></div></section><section className="auth-form-wrap"><div className="auth-panel"><span className="eyebrow">Welcome to PayPilot</span><h2>{mode==='login'?'Good to see you again.':'Create your account.'}</h2><p className="muted">{mode==='login'?'Sign in to continue your shopping journey.':'Your personal shopping copilot is ready.'}</p><form onSubmit={submit}><label>Email address<input type="email" placeholder="you@example.com" value={email} required autoComplete="email" onChange={e=>setEmail(e.target.value)}/></label><label>Password<input type="password" placeholder="At least 8 characters" value={password} required minLength={8} autoComplete={mode==='login'?'current-password':'new-password'} onChange={e=>setPassword(e.target.value)}/></label><button className="primary wide" type="submit" disabled={busy}>{busy?'Please wait…':mode==='login'?'Sign in to PayPilot':'Create my account'}<Icon name="chevronRight"/></button></form>{error&&<p className="error alert">{error}</p>}<p className="switch">{mode==='login'?'New to PayPilot? ':'Already have an account? '}<button className="link" onClick={()=>{setMode(mode==='login'?'register':'login');setError(null)}}>{mode==='login'?'Create an account':'Sign in'}</button></p></div></section></main>}
